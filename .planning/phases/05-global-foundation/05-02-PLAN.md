---
phase: 05-global-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - BigCountryLandscaping/src/components/Hero.tsx
  - BigCountryLandscaping/src/app/contact/page.tsx
  - BigCountryLandscaping/src/app/about/page.tsx
  - BigCountryLandscaping/src/app/services/page.tsx
  - BigCountryLandscaping/src/app/reviews/page.tsx
autonomous: true

must_haves:
  truths:
    - "The homepage hero heading scales fluidly from 32px at 375px to 96px at 1440px — no breakpoint jumps"
    - "The contact, about, services, and reviews page headings scale fluidly between 36px and 64px"
  artifacts:
    - path: "BigCountryLandscaping/src/components/Hero.tsx"
      provides: "Homepage hero h1 with fluid clamp() sizing"
      contains: "clamp(2rem,8vw,6rem)"
    - path: "BigCountryLandscaping/src/app/contact/page.tsx"
      provides: "Contact page h1 with fluid clamp() sizing"
      contains: "clamp(2.25rem,6vw,4rem)"
    - path: "BigCountryLandscaping/src/app/about/page.tsx"
      provides: "About page h1 with fluid clamp() sizing"
      contains: "clamp(2.25rem,6vw,4rem)"
    - path: "BigCountryLandscaping/src/app/services/page.tsx"
      provides: "Services page h1 with fluid clamp() sizing"
      contains: "clamp(2.25rem,6vw,4rem)"
    - path: "BigCountryLandscaping/src/app/reviews/page.tsx"
      provides: "Reviews page h1 with fluid clamp() sizing"
      contains: "clamp(2.25rem,6vw,4rem)"
  key_links:
    - from: "Hero.tsx h1"
      to: "mobile viewport"
      via: "text-[clamp(2rem,8vw,6rem)] replaces 5 step-based responsive classes"
      pattern: "clamp\\(2rem,8vw,6rem\\)"
---

<objective>
Convert all remaining step-based h1 headings to fluid clamp() values using Tailwind v3 arbitrary value syntax.

Purpose: Phase 5 success criterion 3 — "All headings scale fluidly between 375px and 1440px — no fixed-size jumps at breakpoints." Service page h1s already use clamp() and must NOT be touched. This plan covers: Hero.tsx and the four simple page-header h1s.

Output: Five files updated with clamp() Tailwind classes. ExcavationPage, SnowRemovalPage, and LandscapeConstruction are explicitly excluded — they already use clamp() correctly.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-global-foundation/05-RESEARCH.md
@BigCountryLandscaping/src/components/Hero.tsx
@BigCountryLandscaping/src/app/contact/page.tsx
@BigCountryLandscaping/src/app/about/page.tsx
@BigCountryLandscaping/src/app/services/page.tsx
@BigCountryLandscaping/src/app/reviews/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert Hero.tsx h1 to fluid clamp()</name>
  <files>BigCountryLandscaping/src/components/Hero.tsx</files>
  <action>
Read Hero.tsx and locate the `<h1>` element. It currently has this className fragment:
```
text-3xl sm:text-4xl md:text-5xl lg:text-6xl xl:text-7xl
```

Replace those five step classes with one Tailwind arbitrary value class:
```
text-[clamp(2rem,8vw,6rem)]
```

IMPORTANT: No spaces inside the brackets — Tailwind v3 will not generate a class with spaces in arbitrary values.

The h1 contains two `<span className="block">` children. The font-size is on the `<h1>` tag itself, NOT on the spans — spans inherit the size. Confirm you are editing the h1's className, not the spans.

The remaining classes on h1 (`font-black text-white leading-[1.1] tracking-tight mb-5 md:mb-6`) must be preserved exactly as-is.

Full updated h1 opening tag:
```tsx
<h1 className="text-[clamp(2rem,8vw,6rem)] font-black text-white leading-[1.1] tracking-tight mb-5 md:mb-6">
```

DO NOT modify ExcavationPage.tsx, SnowRemovalPage.tsx, or LandscapeConstruction.tsx — those already use `text-[clamp(3rem,8vw,5.5rem)]` and are correct.
  </action>
  <verify>
```bash
grep -n "clamp\|text-3xl\|text-4xl.*text-5xl" BigCountryLandscaping/src/components/Hero.tsx
```
Must show `clamp(2rem,8vw,6rem)` on the h1 line. Must NOT show `text-3xl sm:text-4xl md:text-5xl lg:text-6xl xl:text-7xl` any longer.
  </verify>
  <done>Hero.tsx h1 uses `text-[clamp(2rem,8vw,6rem)]` — all five step-based responsive text classes removed from that element.</done>
</task>

<task type="auto">
  <name>Task 2: Convert page-level h1s on contact, about, services, reviews pages</name>
  <files>
    BigCountryLandscaping/src/app/contact/page.tsx
    BigCountryLandscaping/src/app/about/page.tsx
    BigCountryLandscaping/src/app/services/page.tsx
    BigCountryLandscaping/src/app/reviews/page.tsx
  </files>
  <action>
Read each of the four page files and locate their h1 heading. Each currently has step-based classes in this pattern:
```
text-4xl md:text-5xl lg:text-6xl
```

Replace those three step classes with one class:
```
text-[clamp(2.25rem,6vw,4rem)]
```

No spaces inside brackets.

For each file, preserve ALL other className tokens on the h1 exactly. Only remove the step-based size tokens (`text-4xl`, `md:text-5xl`, `lg:text-6xl`) and insert `text-[clamp(2.25rem,6vw,4rem)]` in their place.

Read each file individually before editing — the surrounding structure may differ but the h1 pattern is consistent across all four.

Then run the build to confirm all four edits compile cleanly:
```bash
cd BigCountryLandscaping && npm run build 2>&1 | tail -30
```
  </action>
  <verify>
```bash
grep -rn "text-4xl md:text-5xl lg:text-6xl" BigCountryLandscaping/src/app/contact/ BigCountryLandscaping/src/app/about/ BigCountryLandscaping/src/app/services/ BigCountryLandscaping/src/app/reviews/
```
Must return no matches — the step-based classes are fully replaced.

```bash
grep -rn "clamp(2.25rem" BigCountryLandscaping/src/app/contact/ BigCountryLandscaping/src/app/about/ BigCountryLandscaping/src/app/services/ BigCountryLandscaping/src/app/reviews/
```
Must return 4 matches (one per file).

```bash
cd BigCountryLandscaping && npm run build 2>&1 | tail -10
```
Build must complete with 0 errors.
  </verify>
  <done>
All four page-level h1s (contact, about, services, reviews) use `text-[clamp(2.25rem,6vw,4rem)]`. No step-based responsive size classes remain on those h1 elements. Build passes.
  </done>
</task>

</tasks>

<verification>
```bash
cd BigCountryLandscaping && npm run build 2>&1 | tail -10
```
Must show successful build with 0 errors and all 14 static routes.

Quick heading audit — confirm no step-based h1 patterns remain in homepage and page files:
```bash
grep -rn "text-3xl sm:text-4xl\|text-4xl md:text-5xl lg:text-6xl" BigCountryLandscaping/src/components/Hero.tsx BigCountryLandscaping/src/app/contact/ BigCountryLandscaping/src/app/about/ BigCountryLandscaping/src/app/services/ BigCountryLandscaping/src/app/reviews/
```
Expected: 0 matches.
</verification>

<success_criteria>
- Hero.tsx h1: `text-[clamp(2rem,8vw,6rem)]` — five step classes removed
- contact/page.tsx h1: `text-[clamp(2.25rem,6vw,4rem)]` — three step classes removed
- about/page.tsx h1: `text-[clamp(2.25rem,6vw,4rem)]` — three step classes removed
- services/page.tsx h1: `text-[clamp(2.25rem,6vw,4rem)]` — three step classes removed
- reviews/page.tsx h1: `text-[clamp(2.25rem,6vw,4rem)]` — three step classes removed
- ExcavationPage.tsx, SnowRemovalPage.tsx, LandscapeConstruction.tsx: NOT touched (already correct)
- Build: 0 errors, all 14 routes
</success_criteria>

<output>
After completion, create `.planning/phases/05-global-foundation/05-02-SUMMARY.md`
</output>
