---
phase: 05-global-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - BigCountryLandscaping/src/components/ServicePillars.tsx
  - BigCountryLandscaping/src/components/Services.tsx
  - BigCountryLandscaping/src/components/ServicesPreview.tsx
  - BigCountryLandscaping/src/components/About.tsx
  - BigCountryLandscaping/src/components/AboutPreview.tsx
  - BigCountryLandscaping/src/components/Contact.tsx
  - BigCountryLandscaping/src/components/CTABanner.tsx
  - BigCountryLandscaping/src/components/CTASection.tsx
  - BigCountryLandscaping/src/components/Testimonials.tsx
  - BigCountryLandscaping/src/components/TestimonialsPreview.tsx
  - BigCountryLandscaping/src/components/WhyBigCountry.tsx
  - BigCountryLandscaping/src/components/WhyChooseUs.tsx
autonomous: true

must_haves:
  truths:
    - "Section headings on the homepage scale fluidly from 24-30px at 375px up to 48px at desktop — no breakpoint jumps"
    - "CTA and testimonial headings scale fluidly — bold and readable at mobile, proportionally larger at desktop"
  artifacts:
    - path: "BigCountryLandscaping/src/components/ServicePillars.tsx"
      provides: "Section h2 with fluid clamp() sizing"
      contains: "clamp(1.5rem,4vw,3rem)"
    - path: "BigCountryLandscaping/src/components/CTABanner.tsx"
      provides: "CTA h2 with fluid clamp() sizing"
      contains: "clamp(2rem,4vw,3rem)"
    - path: "BigCountryLandscaping/src/components/ServicesPreview.tsx"
      provides: "Services preview h2 with fluid clamp() sizing"
      contains: "clamp(1.875rem,4vw,3rem)"
  key_links:
    - from: "12 homepage component h2s"
      to: "fluid viewport scaling"
      via: "text-[clamp(...)] Tailwind arbitrary value replaces step-based responsive classes"
      pattern: "clamp\\(1\\.5rem,4vw,3rem\\)|clamp\\(1\\.875rem,4vw,3rem\\)|clamp\\(2rem,4vw,3rem\\)"
---

<objective>
Convert the 12 homepage component section h2 headings from step-based Tailwind responsive classes to fluid clamp() values.

Purpose: Phase 5 success criterion 3 — all headings fluid between 375px and 1440px. The homepage components are the remaining gap. Service page components (ExcavationPage, SnowRemovalPage, LandscapeConstruction) already use clamp() via inline styles and must NOT be touched.

Output: 12 component files updated. Each section h2's step-based size classes replaced with the appropriate Tailwind arbitrary clamp value from the research table.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-global-foundation/05-RESEARCH.md
@BigCountryLandscaping/src/components/ServicePillars.tsx
@BigCountryLandscaping/src/components/Services.tsx
@BigCountryLandscaping/src/components/ServicesPreview.tsx
@BigCountryLandscaping/src/components/About.tsx
@BigCountryLandscaping/src/components/AboutPreview.tsx
@BigCountryLandscaping/src/components/Contact.tsx
@BigCountryLandscaping/src/components/CTABanner.tsx
@BigCountryLandscaping/src/components/CTASection.tsx
@BigCountryLandscaping/src/components/Testimonials.tsx
@BigCountryLandscaping/src/components/TestimonialsPreview.tsx
@BigCountryLandscaping/src/components/WhyBigCountry.tsx
@BigCountryLandscaping/src/components/WhyChooseUs.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert section h2s in 8 components that use the text-2xl step pattern</name>
  <files>
    BigCountryLandscaping/src/components/ServicePillars.tsx
    BigCountryLandscaping/src/components/Services.tsx
    BigCountryLandscaping/src/components/About.tsx
    BigCountryLandscaping/src/components/Contact.tsx
    BigCountryLandscaping/src/components/Testimonials.tsx
    BigCountryLandscaping/src/components/WhyBigCountry.tsx
    BigCountryLandscaping/src/components/WhyChooseUs.tsx
    BigCountryLandscaping/src/components/CTABanner.tsx
  </files>
  <action>
Read each file and locate its section heading h2. Apply the replacement shown in the table below.

For each file, the edit is: find the step-based size classes on the h2 and replace with the clamp value. Preserve all other classes on the h2 unchanged.

**Replacement table:**

| File | Current size classes on h2 | Replace with |
|------|---------------------------|--------------|
| ServicePillars.tsx | `text-2xl sm:text-3xl md:text-4xl lg:text-5xl` | `text-[clamp(1.5rem,4vw,3rem)]` |
| Services.tsx | `text-2xl sm:text-3xl md:text-4xl lg:text-5xl` | `text-[clamp(1.5rem,4vw,3rem)]` |
| About.tsx | `text-2xl sm:text-3xl md:text-4xl lg:text-5xl` | `text-[clamp(1.5rem,4vw,3rem)]` |
| Contact.tsx | `text-2xl sm:text-3xl md:text-4xl lg:text-5xl` | `text-[clamp(1.5rem,4vw,3rem)]` |
| Testimonials.tsx | `text-2xl sm:text-3xl md:text-4xl lg:text-5xl` | `text-[clamp(1.5rem,4vw,3rem)]` |
| WhyBigCountry.tsx | `text-2xl sm:text-3xl md:text-4xl lg:text-5xl` | `text-[clamp(1.5rem,4vw,3rem)]` |
| WhyChooseUs.tsx | `text-2xl sm:text-3xl md:text-4xl lg:text-5xl` | `text-[clamp(1.5rem,4vw,3rem)]` |
| CTABanner.tsx | `text-4xl md:text-5xl` | `text-[clamp(2rem,4vw,3rem)]` |

IMPORTANT: No spaces inside the clamp brackets — `text-[clamp(1.5rem,4vw,3rem)]` not `text-[clamp(1.5rem, 4vw, 3rem)]`.

Read each file first to confirm the exact current class string before editing. If a file uses a slightly different step pattern than listed, match the actual current content and replace the size classes with the appropriate clamp value from the table.

Each component may have more than one heading element. Edit only the PRIMARY section heading h2 (the large title at the top of the section). Do not edit card titles, stat numbers, or other smaller headings within the component — only the section-level h2.

After all 8 edits, run a quick check:
```bash
grep -n "clamp" BigCountryLandscaping/src/components/ServicePillars.tsx BigCountryLandscaping/src/components/Services.tsx BigCountryLandscaping/src/components/About.tsx BigCountryLandscaping/src/components/Contact.tsx BigCountryLandscaping/src/components/Testimonials.tsx BigCountryLandscaping/src/components/WhyBigCountry.tsx BigCountryLandscaping/src/components/WhyChooseUs.tsx BigCountryLandscaping/src/components/CTABanner.tsx
```
Should show 8 matches (one per file).
  </action>
  <verify>
```bash
grep -rn "text-2xl sm:text-3xl md:text-4xl lg:text-5xl\|text-4xl md:text-5xl$" BigCountryLandscaping/src/components/ServicePillars.tsx BigCountryLandscaping/src/components/Services.tsx BigCountryLandscaping/src/components/About.tsx BigCountryLandscaping/src/components/Contact.tsx BigCountryLandscaping/src/components/Testimonials.tsx BigCountryLandscaping/src/components/WhyBigCountry.tsx BigCountryLandscaping/src/components/WhyChooseUs.tsx BigCountryLandscaping/src/components/CTABanner.tsx
```
Must return 0 matches — step-based size classes on section h2s are fully replaced.
  </verify>
  <done>All 8 components have their section h2 step classes replaced with the appropriate `text-[clamp(...)]` value from the table. No step-based responsive size classes remain on those h2 elements.</done>
</task>

<task type="auto">
  <name>Task 2: Convert section h2s in 4 components that use the text-3xl step pattern, then verify build</name>
  <files>
    BigCountryLandscaping/src/components/ServicesPreview.tsx
    BigCountryLandscaping/src/components/AboutPreview.tsx
    BigCountryLandscaping/src/components/CTASection.tsx
    BigCountryLandscaping/src/components/TestimonialsPreview.tsx
  </files>
  <action>
Read each file and locate its section heading h2. Apply the replacement shown below.

**Replacement table:**

| File | Current size classes on h2 | Replace with |
|------|---------------------------|--------------|
| ServicesPreview.tsx | `text-3xl md:text-4xl lg:text-5xl` | `text-[clamp(1.875rem,4vw,3rem)]` |
| AboutPreview.tsx | `text-3xl md:text-4xl lg:text-5xl` | `text-[clamp(1.875rem,4vw,3rem)]` |
| CTASection.tsx | `text-3xl md:text-4xl lg:text-5xl` | `text-[clamp(1.875rem,4vw,3rem)]` |
| TestimonialsPreview.tsx | `text-3xl md:text-4xl lg:text-5xl` | `text-[clamp(1.875rem,4vw,3rem)]` |

No spaces inside brackets. Read each file before editing to confirm the actual current class string.

Edit only the PRIMARY section h2. Do not modify card titles, secondary headings, or other text elements.

After the 4 edits, run the full project build:
```bash
cd BigCountryLandscaping && npm run build 2>&1 | tail -30
```
Build must pass with 0 errors.
  </action>
  <verify>
```bash
grep -rn "text-3xl md:text-4xl lg:text-5xl" BigCountryLandscaping/src/components/ServicesPreview.tsx BigCountryLandscaping/src/components/AboutPreview.tsx BigCountryLandscaping/src/components/CTASection.tsx BigCountryLandscaping/src/components/TestimonialsPreview.tsx
```
Must return 0 matches.

```bash
cd BigCountryLandscaping && npm run build 2>&1 | tail -10
```
Must show successful build with 0 errors.
  </verify>
  <done>
All 4 preview/section components have their h2 step classes replaced with `text-[clamp(1.875rem,4vw,3rem)]`. Build passes with 0 errors. All 14 static routes listed in build output.
  </done>
</task>

</tasks>

<verification>
Full heading audit — confirm no step-based section h2 patterns remain in any of the 12 components:
```bash
grep -rn "text-2xl sm:text-3xl md:text-4xl\|text-3xl md:text-4xl lg:text-5xl\|text-4xl md:text-5xl" BigCountryLandscaping/src/components/ServicePillars.tsx BigCountryLandscaping/src/components/Services.tsx BigCountryLandscaping/src/components/ServicesPreview.tsx BigCountryLandscaping/src/components/About.tsx BigCountryLandscaping/src/components/AboutPreview.tsx BigCountryLandscaping/src/components/Contact.tsx BigCountryLandscaping/src/components/CTABanner.tsx BigCountryLandscaping/src/components/CTASection.tsx BigCountryLandscaping/src/components/Testimonials.tsx BigCountryLandscaping/src/components/TestimonialsPreview.tsx BigCountryLandscaping/src/components/WhyBigCountry.tsx BigCountryLandscaping/src/components/WhyChooseUs.tsx
```
Expected: 0 matches.

Confirm clamp values present (should return 12 lines):
```bash
grep -rn "clamp" BigCountryLandscaping/src/components/ServicePillars.tsx BigCountryLandscaping/src/components/Services.tsx BigCountryLandscaping/src/components/ServicesPreview.tsx BigCountryLandscaping/src/components/About.tsx BigCountryLandscaping/src/components/AboutPreview.tsx BigCountryLandscaping/src/components/Contact.tsx BigCountryLandscaping/src/components/CTABanner.tsx BigCountryLandscaping/src/components/CTASection.tsx BigCountryLandscaping/src/components/Testimonials.tsx BigCountryLandscaping/src/components/TestimonialsPreview.tsx BigCountryLandscaping/src/components/WhyBigCountry.tsx BigCountryLandscaping/src/components/WhyChooseUs.tsx
```

Final build:
```bash
cd BigCountryLandscaping && npm run build 2>&1 | tail -10
```
0 errors, all 14 routes.
</verification>

<success_criteria>
- ServicePillars.tsx, Services.tsx, About.tsx, Contact.tsx, Testimonials.tsx, WhyBigCountry.tsx, WhyChooseUs.tsx: section h2 uses `text-[clamp(1.5rem,4vw,3rem)]`
- CTABanner.tsx: h2 uses `text-[clamp(2rem,4vw,3rem)]`
- ServicesPreview.tsx, AboutPreview.tsx, CTASection.tsx, TestimonialsPreview.tsx: h2 uses `text-[clamp(1.875rem,4vw,3rem)]`
- ExcavationPage.tsx, SnowRemovalPage.tsx, LandscapeConstruction.tsx: NOT modified
- Build: 0 errors, all 14 routes
- MTYP-04 satisfied: all homepage section h2s now use fluid clamp() scaling
</success_criteria>

<output>
After completion, create `.planning/phases/05-global-foundation/05-03-SUMMARY.md`
</output>
