---
phase: 04-dark-theme-homepage
plan: "02"
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - abc-roofing/src/components/Header.tsx
  - abc-roofing/public/images/hero-excavator.jpg
  - abc-roofing/public/images/excavation-pillar.jpg
  - abc-roofing/public/images/snow-pillar.jpg
autonomous: true

must_haves:
  truths:
    - "The header is always a dark semi-transparent bar — it never turns white on scroll"
    - "Nav links are white/80 by default, amber (#F5A623) on active route and on hover"
    - "The mobile menu opens as a full-screen dark (#0f1f2d) overlay with large white nav links and amber borders"
    - "The hamburger button is always white (never dark text on a light bg)"
    - "Three client photos exist in abc-roofing/public/images/ and are ready for next/image use"
  artifacts:
    - path: "abc-roofing/src/components/Header.tsx"
      provides: "Always-dark sticky header with dark mobile menu overlay"
      contains: "bg-[#0f1f2d]/95"
    - path: "abc-roofing/public/images/hero-excavator.jpg"
      provides: "Hero background photo"
    - path: "abc-roofing/public/images/excavation-pillar.jpg"
      provides: "Excavation service pillar photo"
    - path: "abc-roofing/public/images/snow-pillar.jpg"
      provides: "Snow removal service pillar photo"
  key_links:
    - from: "abc-roofing/src/components/Header.tsx"
      to: "header element className"
      via: "isScrolled conditional"
      pattern: "bg-\\[#0f1f2d\\]"
    - from: "abc-roofing/src/components/Header.tsx"
      to: "mobile menu backdrop div"
      via: "absolute inset-0"
      pattern: "bg-\\[#0f1f2d\\]"
---

<objective>
Rebuild Header.tsx so it stays dark at all times (no transparent-to-white scroll transition), and rebuild the mobile menu as a full-screen dark overlay. Stage the three client photos into abc-roofing/public/images/ so Plans 03 and 04 can reference them.

Purpose: The header is visible on every scroll position on every page — a white header breaks the dark industrial brand on every single interaction. Photo staging unblocks the two most visually impactful sections (Hero, ServicePillars).
Output: Header.tsx with always-dark behavior; public/images/ with three photo files.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-dark-theme-homepage/04-RESEARCH.md
@abc-roofing/src/components/Header.tsx
@.planning/phases/04-dark-theme-homepage/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stage client photos into public/images/</name>
  <files>
    abc-roofing/public/images/hero-excavator.jpg
    abc-roofing/public/images/excavation-pillar.jpg
    abc-roofing/public/images/snow-pillar.jpg
  </files>
  <action>
Create the directory `abc-roofing/public/images/` if it does not exist.

The root of the project contains four client photos (visible in git status). Identify which is which:
- `big country.jpg` — likely the most general/branded shot; use as `hero-excavator.jpg`
- `504077954_17962441439946529_3928592317949450129_n.jpg` — copy as `excavation-pillar.jpg`
- `504342873_17962441421946529_7714374767995459450_n.jpg` — copy as `snow-pillar.jpg`
- `428682832_393139593467094_8568585701890923745_n.jpg` — reserve as backup; do not use in Phase 4

Copy (do not move) the three files into abc-roofing/public/images/ with the clean names above. Use Bash cp commands with properly quoted paths since source filenames contain spaces and long numbers.

After copying, verify all three destination files exist and are non-zero size.

Note: If photo content doesn't clearly match service (e.g., a snow photo ends up as hero-excavator), that's acceptable for Phase 4 — correctness of image placement is verified in the human checkpoint in Plan 07. The key requirement is that three photo files exist at those exact paths so next/image can serve them.
  </action>
  <verify>
Run: `ls -la abc-roofing/public/images/` — must show hero-excavator.jpg, excavation-pillar.jpg, snow-pillar.jpg all with size > 0.
  </verify>
  <done>Three jpg files exist in abc-roofing/public/images/ with the exact filenames hero-excavator.jpg, excavation-pillar.jpg, snow-pillar.jpg.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild Header.tsx as always-dark with dark mobile overlay</name>
  <files>abc-roofing/src/components/Header.tsx</files>
  <action>
Rewrite Header.tsx. Keep all existing logic intact: useEffect scroll listener, useEffect body overflow lock, navLinks array, isActive helper, isMobileMenuOpen state, pathname usage. Only change the className strings and one structural change to the mobile menu.

**Header element (the `&lt;header&gt;` tag, currently line 45-51):**
Replace the className conditional:
```
// FROM:
isScrolled
  ? "bg-white shadow-lg shadow-black/5 py-2 md:py-3"
  : "bg-transparent py-3 md:py-5"

// TO:
isScrolled
  ? "bg-[#0f1f2d]/95 backdrop-blur-sm shadow-lg shadow-black/30 py-2 md:py-3"
  : "bg-[#0f1f2d]/80 backdrop-blur-sm py-3 md:py-5"
```

**Logo text color (the "Big Country Landscaping" span):**
Replace the conditional:
```
// FROM:
isScrolled || isMobileMenuOpen ? "text-[#0f1f2d]" : "text-white"

// TO:
"text-white"
```
(Remove the conditional entirely — always white.)

**Desktop nav link color (inside navLinks.map):**
Replace:
```
// FROM:
isActive(link.href)
  ? "text-[#F5A623]"
  : isScrolled
    ? "text-[#0f1f2d] hover:text-[#F5A623]"
    : "text-white/90 hover:text-[#F5A623]"

// TO:
isActive(link.href)
  ? "text-[#F5A623]"
  : "text-white/80 hover:text-[#F5A623]"
```

**Phone link color (desktop, the `&lt;a href="tel:..."&gt;`):**
Replace:
```
// FROM:
isScrolled ? "text-[#0f1f2d] hover:text-[#F5A623]" : "text-white hover:text-[#F5A623]"

// TO:
"text-white hover:text-[#F5A623]"
```

**Mobile menu button:**
Replace:
```
// FROM:
isScrolled || isMobileMenuOpen
  ? "text-[#0f1f2d] hover:bg-gray-100"
  : "text-white hover:bg-white/10"

// TO:
"text-white hover:bg-white/10"
```

**Mobile menu backdrop div (currently `absolute inset-0 bg-white`):**
Change to:
```tsx
<div
  className="absolute inset-0 bg-[#0f1f2d]"
  onClick={() => setIsMobileMenuOpen(false)}
/>
```

**Mobile menu nav links (inside navLinks.map for mobile, currently text-[#0f1f2d]):**
Replace:
```
// FROM:
isActive(link.href)
  ? "text-[#F5A623]"
  : "text-[#0f1f2d] active:text-[#F5A623]"

className includes: "border-gray-100"

// TO:
isActive(link.href)
  ? "text-[#F5A623]"
  : "text-white active:text-[#F5A623]"

className: replace "border-gray-100" with "border-white/10"
Also change "font-bold" to "font-black" for industrial weight
```

**Mobile menu bottom actions section:**
- `border-t border-gray-200` → `border-t border-white/10`
- Phone button: `text-[#0f1f2d] bg-gray-100 active:bg-gray-200` → `text-white bg-white/10 active:bg-white/20`

Keep `isScrolled` state and the scroll useEffect — still needed for the subtle opacity/blur change on scroll. Keep `isMobileMenuOpen` and all its logic. Keep `z-50` on header, `z-40` on mobile menu panel. Keep `opacity-100 visible` / `opacity-0 invisible` toggle on the mobile menu outer div. Keep `translate-x-full / translate-x-0` slide animation on the menu content div — it still works with the dark overlay.
  </action>
  <verify>
Run: `cd abc-roofing && npm run build` — must complete with no TypeScript errors. Then visually scan Header.tsx for any remaining `bg-white`, `text-[#0f1f2d]` (in color conditionals), or `border-gray-200` / `border-gray-100` — none should remain in the scroll/mobile-menu color conditionals (they may remain in other unrelated contexts if any).
  </verify>
  <done>Header is always dark. Mobile menu opens as dark overlay. No `bg-white` or `bg-transparent` in the header element className. Build passes.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `ls -la abc-roofing/public/images/` shows three non-empty jpg files
2. `cd abc-roofing && npm run build` passes
3. Grep Header.tsx for `bg-white` — must return 0 results
4. Grep Header.tsx for `"text-\[#0f1f2d\]"` in conditional expressions — must return 0 results (the logo subtitle `text-[#F5A623]` is fine, and btn-primary uses #0f1f2d internally but that's in globals)
</verification>

<success_criteria>
- Three photos exist in abc-roofing/public/images/ at exact required filenames
- Header always dark: `bg-[#0f1f2d]/95` scrolled, `bg-[#0f1f2d]/80` unscrolled
- Mobile menu backdrop is `bg-[#0f1f2d]` (not bg-white)
- Mobile nav links are white text on dark, amber on active
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/04-dark-theme-homepage/04-02-SUMMARY.md` using the summary template.
</output>
