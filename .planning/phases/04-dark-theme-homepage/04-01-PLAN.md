---
phase: 04-dark-theme-homepage
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - abc-roofing/src/app/globals.css
  - abc-roofing/src/app/layout.tsx
autonomous: true

must_haves:
  truths:
    - "Opening any page of the site shows a near-black (#0f1f2d) background immediately on load — no white flash"
    - "The browser scrollbar track is dark and thumb is amber — consistent with the dark page"
    - "Browser chrome (scrollbar, system date pickers) respects the dark colorScheme"
  artifacts:
    - path: "abc-roofing/src/app/globals.css"
      provides: "Dark body background and dark sticky-cta-mobile utility"
      contains: "background: #0f1f2d"
    - path: "abc-roofing/src/app/layout.tsx"
      provides: "Dark colorScheme viewport export"
      contains: "colorScheme: \"dark\""
  key_links:
    - from: "abc-roofing/src/app/globals.css"
      to: "body"
      via: "CSS rule parsed at stylesheet load"
      pattern: "background: #0f1f2d"
    - from: "abc-roofing/src/app/layout.tsx"
      to: "viewport export"
      via: "Next.js Viewport export"
      pattern: "colorScheme.*dark"
---

<objective>
Flip the global body background from white to near-black and update the viewport colorScheme to dark. This is the highest-leverage single change in Phase 4 — it makes the entire site dark-by-default at CSS parse time, before any JavaScript runs, eliminating the white flash on load.

Purpose: Every downstream component change in this phase depends on the dark default. Doing this first means the other plans work on a dark canvas from the start.
Output: globals.css with dark body, dark sticky-cta-mobile utility; layout.tsx with colorScheme: "dark".
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-dark-theme-homepage/04-RESEARCH.md
@abc-roofing/src/app/globals.css
@abc-roofing/src/app/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip body to dark in globals.css</name>
  <files>abc-roofing/src/app/globals.css</files>
  <action>
Make exactly two changes to the body rule (lines 36-47):
1. Change `color: #0f1f2d;` to `color: #ffffff;`
2. Change `background: #ffffff;` to `background: #0f1f2d;`

Then update the `.sticky-cta-mobile` utility class (around line 209-212):
- Change `bg-white` to remove it (the MobileStickyCTA component will set its own dark bg)
- Change `border-gray-200` to `border-white/10`
The updated rule should be:
```css
.sticky-cta-mobile {
  @apply fixed bottom-0 left-0 right-0 z-40 bg-[#0f1f2d] border-t border-white/10 p-3 shadow-lg;
  padding-bottom: max(0.75rem, env(safe-area-inset-bottom));
}
```

Do NOT touch any other rules — the scrollbar track (already dark at #0f1f2d), scrollbar thumb (already amber), or any component utilities. Only the body background/color and sticky-cta-mobile need changes.
  </action>
  <verify>Search globals.css for `background: #ffffff` — must return zero matches. Search for `background: #0f1f2d` in the body rule — must be present.</verify>
  <done>body has `color: #ffffff; background: #0f1f2d;`. sticky-cta-mobile has `bg-[#0f1f2d] border-t border-white/10`.</done>
</task>

<task type="auto">
  <name>Task 2: Set colorScheme to dark in layout.tsx</name>
  <files>abc-roofing/src/app/layout.tsx</files>
  <action>
In the `viewport` export object (around line 16-23), change the single value:
- `colorScheme: "light"` → `colorScheme: "dark"`

No other changes to layout.tsx. The metadata, font loading, component imports, and JSX structure are all correct and must not be touched.
  </action>
  <verify>Search layout.tsx for `colorScheme` — must show `"dark"` not `"light"`.</verify>
  <done>viewport export has `colorScheme: "dark"`.</done>
</task>

</tasks>

<verification>
Run `cd abc-roofing && npm run build` — must complete with no TypeScript errors. The two changed files are CSS and a simple object property change — no logic risk.
</verification>

<success_criteria>
- globals.css body: `color: #ffffff; background: #0f1f2d` confirmed
- layout.tsx viewport: `colorScheme: "dark"` confirmed
- npm run build passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-dark-theme-homepage/04-01-SUMMARY.md` using the summary template.
</output>
