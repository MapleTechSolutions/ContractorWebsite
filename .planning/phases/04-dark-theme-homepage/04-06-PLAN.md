---
phase: 04-dark-theme-homepage
plan: "06"
type: execute
wave: 4
depends_on: ["04-01"]
files_modified:
  - abc-roofing/src/components/Testimonials.tsx
autonomous: true

must_haves:
  truths:
    - "The testimonials section has a dark near-black background — not bg-white"
    - "Testimonial cards use a dark glass style: bg-white/5 border border-white/10"
    - "Quote text is white/80, author name is white, location is white/50"
    - "Star ratings are amber (#F5A623)"
    - "The slider navigation arrows and dots use dark-appropriate colors (white borders/bg instead of gray-200/gray-100)"
    - "The reviews badge at the bottom has a dark bg — not bg-[#f8f9fa]"
  artifacts:
    - path: "abc-roofing/src/components/Testimonials.tsx"
      provides: "Dark-themed testimonials slider"
      contains: "bg-[#0f1f2d]"
      exports: ["default function Testimonials"]
  key_links:
    - from: "abc-roofing/src/components/Testimonials.tsx"
      to: "section element"
      via: "className prop"
      pattern: "bg-\\[#0f1f2d\\]"
---

<objective>
Reskin the existing Testimonials.tsx from its current light theme (bg-white, bg-[#f8f9fa], gray-* borders, text-gray-700) to the dark industrial theme. The slider logic, testimonial data, auto-advance behavior, and component structure are all preserved verbatim — only color values change.

Purpose: HOME-06 requires testimonials on a dark background. Testimonials.tsx already exists with the correct slider component (4 testimonials, auto-advance, mobile touch, dots) but uses a light color palette throughout.
Output: Testimonials.tsx with dark color values throughout, slider logic untouched.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-dark-theme-homepage/04-RESEARCH.md
@abc-roofing/src/components/Testimonials.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip Testimonials.tsx from light to dark color values</name>
  <files>abc-roofing/src/components/Testimonials.tsx</files>
  <action>
Read Testimonials.tsx fully. Then make targeted color substitutions. Do NOT change: testimonials data array, useState/useEffect/useCallback logic, goToNext/goToPrev functions, interval logic, JSX structure, aria-labels, or animation classes.

**Section element (line ~59):**
`bg-white` → `bg-[#0f1f2d]`

**Section header pill badge (line ~63-66):**
`bg-[#F5A623]/10 text-[#0f1f2d]` → `bg-white/10 text-[#F5A623]`

**Section heading (line ~69-71):**
`text-[#0f1f2d]` → `text-white`

**Section subheading (line ~73-75):**
`text-gray-600` → `text-white/60`

**Testimonial card wrapper div (line ~81, the `relative bg-[#f8f9fa] rounded-2xl...`):**
`bg-[#f8f9fa]` → `bg-white/5`
`border border-gray-100` → `border border-white/10`
`shadow-xl shadow-gray-200/50` → `shadow-xl shadow-black/30`

**Testimonial text paragraphs (all `text-gray-700` in the slider text):**
`text-gray-700` → `text-white/80`

**Author name divs:**
`font-semibold text-[#0f1f2d]` → `font-semibold text-white`

**Author location divs:**
`text-gray-500` → `text-white/50`

**Mobile navigation prev/next buttons (the `w-12 h-12 rounded-full bg-white border border-gray-200` buttons):**
`bg-white border-gray-200` → `bg-white/10 border-white/20`
Arrow SVG: `text-[#0f1f2d]` → `text-white`

**Mobile dot buttons (inactive state: `bg-gray-300`):**
`bg-gray-300` → `bg-white/20`

**Desktop dot buttons (inactive hover: `hover:bg-gray-400`):**
`bg-gray-300 hover:bg-gray-400` → `bg-white/20 hover:bg-white/40`

**Reviews badge at bottom (line ~200-213, `bg-[#f8f9fa] border border-gray-200`):**
`bg-[#f8f9fa] border border-gray-200` → `bg-white/5 border border-white/10`
`font-bold text-[#0f1f2d]` (the 5.0 rating number) → `font-bold text-white`
`text-gray-600` (the "Based on 100+ reviews" text) → `text-white/60`
The divider: `bg-gray-300` → `bg-white/20`
`font-semibold text-[#0f1f2d]` (the "100+" span) → `font-semibold text-white`

Keep `"use client"` directive — this component requires useState/useEffect for slider logic.
Keep the quote decoration div (amber square with quotation marks) — already correctly styled.
  </action>
  <verify>
`cd abc-roofing && npm run build` passes. Grep Testimonials.tsx for `bg-white` (not part of `bg-white/5` or `bg-white/10`) — must return 0 results matching standalone `bg-white`. Grep for `bg-\[#f8f9fa\]` — must return 0 results. Grep for `text-gray-700` — must return 0 results. Grep for `bg-\[#0f1f2d\]` on the section — must be present.
  </verify>
  <done>Testimonials.tsx has dark section bg, dark card backgrounds (bg-white/5), white text, dark navigation arrows and dots. Slider logic unchanged. Build passes.</done>
</task>

</tasks>

<verification>
1. `cd abc-roofing && npm run build` passes
2. `grep "bg-\[#f8f9fa\]" abc-roofing/src/components/Testimonials.tsx` → 0 results
3. `grep "bg-white[^/]" abc-roofing/src/components/Testimonials.tsx` → 0 results (no standalone bg-white)
4. `grep "text-gray-700" abc-roofing/src/components/Testimonials.tsx` → 0 results
5. `grep "bg-\[#0f1f2d\]" abc-roofing/src/components/Testimonials.tsx` → at least 1 result (section)
6. "use client" still present
</verification>

<success_criteria>
- Section background: bg-[#0f1f2d] (dark, not white)
- Card background: bg-white/5 with border-white/10
- Text colors: white/80 for quotes, white for names, white/50 for locations
- Nav arrows: dark glass style (white/10 bg)
- Dot indicators: white/20 inactive
- Reviews badge: dark background
- All slider logic (auto-advance, goToNext, goToPrev, isPaused) preserved verbatim
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/04-dark-theme-homepage/04-06-SUMMARY.md` using the summary template.
</output>
