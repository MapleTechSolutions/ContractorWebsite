---
phase: 04-dark-theme-homepage
plan: "04"
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - abc-roofing/src/components/ServicePillars.tsx
autonomous: true

must_haves:
  truths:
    - "Two equal service pillars appear side-by-side on desktop (md:grid-cols-2) and stacked on mobile"
    - "Each pillar shows a real photo background with a dark-to-transparent gradient overlay anchoring the text at the bottom"
    - "The Excavation pillar lists 5 sub-services; the Snow Removal pillar lists 5 sub-services"
    - "The Snow Removal pillar has a prominent '24/7 Storm Response' amber pill badge (CONV-03)"
    - "Each pillar has a 'Learn More' button linking to /services"
  artifacts:
    - path: "abc-roofing/src/components/ServicePillars.tsx"
      provides: "Two-pillar service section with photo backgrounds and sub-service lists"
      contains: "excavation-pillar.jpg"
      exports: ["default function ServicePillars"]
  key_links:
    - from: "abc-roofing/src/components/ServicePillars.tsx"
      to: "abc-roofing/public/images/excavation-pillar.jpg"
      via: "next/image src prop"
      pattern: "excavation-pillar.jpg"
    - from: "abc-roofing/src/components/ServicePillars.tsx"
      to: "abc-roofing/public/images/snow-pillar.jpg"
      via: "next/image src prop"
      pattern: "snow-pillar.jpg"
---

<objective>
Create ServicePillars.tsx — a new server component that replaces ServicesPreview on the homepage. Two equal photo-backed pillar cards in a side-by-side grid, each with a sub-service list and CTA.

Purpose: HOME-02 requires two dedicated service pillar sections with photos. The existing ServicesPreview uses a light background and icon cards — it does not satisfy the photo pillar requirement and is not reusable. Creating a new component keeps ServicesPreview available on the /services page.
Output: abc-roofing/src/components/ServicePillars.tsx as a server component with photo pillars.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-dark-theme-homepage/04-RESEARCH.md
@abc-roofing/src/components/ServicesPreview.tsx
@.planning/phases/04-dark-theme-homepage/04-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ServicePillars.tsx as a server component</name>
  <files>abc-roofing/src/components/ServicePillars.tsx</files>
  <action>
Create `abc-roofing/src/components/ServicePillars.tsx` as a new server component (no `"use client"` directive).

Imports needed:
```tsx
import Image from "next/image";
import Link from "next/link";
```

Define two service data objects inline (no separate data file needed):

Excavation pillar:
- title: "Excavation & Site Work"
- photo: "/images/excavation-pillar.jpg"
- photoAlt: "Commercial excavation services"
- badge: null (no pill badge)
- items: ["Site Clearing & Grading", "Utility Trenching", "Rough & Fine Grading", "Demolition & Removal", "Foundation Excavation"]

Snow Removal pillar:
- title: "Snow & Ice Removal"
- photo: "/images/snow-pillar.jpg"
- photoAlt: "Commercial snow removal services"
- badge: "24/7 Storm Response" (amber pill — CONV-03)
- items: ["Commercial Lot Clearing", "Sidewalk & Pathway Clearing", "Salting & Sanding", "Snow Hauling & Relocation", "Seasonal Contracts"]

Component structure:
```tsx
export default function ServicePillars() {
  return (
    <section className="section-padding bg-[#0f1f2d]">
      <div className="container-custom">
        {/* Section header */}
        <div className="text-center mb-8 md:mb-12">
          <div className="inline-flex items-center gap-2 bg-white/10 text-[#F5A623] px-4 py-2 rounded-full text-sm font-semibold mb-4">
            Our Services
          </div>
          <h2 className="text-2xl sm:text-3xl md:text-4xl lg:text-5xl font-black text-white tracking-tight">
            Built for <span className="text-[#F5A623]">Commercial Scale</span>
          </h2>
        </div>

        {/* Pillars grid */}
        <div className="grid md:grid-cols-2 gap-6">
          {/* Pillar: Excavation */}
          <div className="relative rounded-2xl overflow-hidden min-h-[420px] md:min-h-[560px]">
            <Image
              src="/images/excavation-pillar.jpg"
              alt="Commercial excavation services"
              fill
              className="object-cover"
              sizes="(min-width: 768px) 50vw, 100vw"
            />
            {/* Gradient overlay — heavy at bottom for text legibility */}
            <div className="absolute inset-0 bg-gradient-to-t from-[#0f1f2d]/95 via-[#0f1f2d]/50 to-[#0f1f2d]/20" />
            {/* Content anchored to bottom */}
            <div className="absolute inset-0 flex flex-col justify-end p-6 md:p-8">
              <h3 className="text-2xl md:text-3xl font-black text-white tracking-tight mb-3">
                Excavation &amp; Site Work
              </h3>
              <ul className="space-y-1.5 mb-5">
                {["Site Clearing & Grading", "Utility Trenching", "Rough & Fine Grading", "Demolition & Removal", "Foundation Excavation"].map((item) => (
                  <li key={item} className="flex items-center gap-2 text-white/80 text-sm">
                    <span className="text-[#F5A623] font-bold flex-shrink-0">—</span>
                    {item}
                  </li>
                ))}
              </ul>
              <Link href="/services" className="btn-primary w-fit text-sm">
                Learn More
              </Link>
            </div>
          </div>

          {/* Pillar: Snow Removal */}
          <div className="relative rounded-2xl overflow-hidden min-h-[420px] md:min-h-[560px]">
            <Image
              src="/images/snow-pillar.jpg"
              alt="Commercial snow removal services"
              fill
              className="object-cover"
              sizes="(min-width: 768px) 50vw, 100vw"
            />
            <div className="absolute inset-0 bg-gradient-to-t from-[#0f1f2d]/95 via-[#0f1f2d]/50 to-[#0f1f2d]/20" />
            <div className="absolute inset-0 flex flex-col justify-end p-6 md:p-8">
              {/* 24/7 pill badge */}
              <div className="inline-flex items-center gap-2 bg-[#F5A623] text-[#0f1f2d] px-3 py-1 rounded-full text-xs font-black uppercase tracking-wider mb-3 w-fit">
                24/7 Storm Response
              </div>
              <h3 className="text-2xl md:text-3xl font-black text-white tracking-tight mb-3">
                Snow &amp; Ice Removal
              </h3>
              <ul className="space-y-1.5 mb-5">
                {["Commercial Lot Clearing", "Sidewalk & Pathway Clearing", "Salting & Sanding", "Snow Hauling & Relocation", "Seasonal Contracts"].map((item) => (
                  <li key={item} className="flex items-center gap-2 text-white/80 text-sm">
                    <span className="text-[#F5A623] font-bold flex-shrink-0">—</span>
                    {item}
                  </li>
                ))}
              </ul>
              <Link href="/services" className="btn-primary w-fit text-sm">
                Learn More
              </Link>
            </div>
          </div>
        </div>
      </div>
    </section>
  );
}
```

This component is a server component — no `"use client"` directive. No state, no effects. All data is inline.
  </action>
  <verify>
`cd abc-roofing && npm run build` passes. File exists at abc-roofing/src/components/ServicePillars.tsx. Grep for `fill` — must return two Image components. Grep for `snow-pillar.jpg` and `excavation-pillar.jpg` — both must be present. Grep for `24/7 Storm Response` — must be present.
  </verify>
  <done>ServicePillars.tsx exists as a server component with two photo pillars, sub-service lists, 24/7 badge on Snow pillar, and Learn More links. Build passes.</done>
</task>

</tasks>

<verification>
1. `cd abc-roofing && npm run build` passes
2. abc-roofing/src/components/ServicePillars.tsx exists
3. No `"use client"` in ServicePillars.tsx
4. Both photo paths present: /images/excavation-pillar.jpg and /images/snow-pillar.jpg
5. `sizes="(min-width: 768px) 50vw, 100vw"` on both images (not 100vw — they're half-width on desktop)
6. 24/7 Storm Response badge present on Snow pillar
7. Five sub-service items in each pillar
</verification>

<success_criteria>
- ServicePillars.tsx is a server component with no "use client"
- Two photo pillars in md:grid-cols-2 grid
- Each pillar: fill image, gradient overlay, sub-service list, Learn More link
- Snow pillar has 24/7 Storm Response amber pill badge
- Build passes with no TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-dark-theme-homepage/04-04-SUMMARY.md` using the summary template.
</output>
