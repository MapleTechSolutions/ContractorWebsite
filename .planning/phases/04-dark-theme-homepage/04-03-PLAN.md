---
phase: 04-dark-theme-homepage
plan: "03"
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - abc-roofing/src/components/Hero.tsx
autonomous: true

must_haves:
  truths:
    - "The hero section shows a real machine photo as its background — not a CSS gradient"
    - "A dark overlay sits between the photo and the text, making white text clearly legible"
    - "Three visible trust badge pills (WCB Alberta, Licensed & Insured, 24/7 Available) appear below the CTA buttons"
    - "The hero form still shows a loading spinner then a success screen on submit — form logic is untouched"
    - "The hero is the LCP element — photo loads with priority, not lazy"
  artifacts:
    - path: "abc-roofing/src/components/Hero.tsx"
      provides: "Full-bleed machine photo hero with dark overlay and upgraded trust badges"
      contains: "fill"
      exports: ["default function Hero"]
  key_links:
    - from: "abc-roofing/src/components/Hero.tsx"
      to: "abc-roofing/public/images/hero-excavator.jpg"
      via: "next/image src prop"
      pattern: "src=\"/images/hero-excavator.jpg\""
    - from: "abc-roofing/src/components/Hero.tsx"
      to: "next/image"
      via: "fill + priority props"
      pattern: "priority"
---

<objective>
Replace the Hero's CSS gradient background with a full-bleed machine photo using next/image fill pattern, add a dark overlay for text legibility, and upgrade the trust indicators from plain text to pill badges (WCB Alberta, Licensed & Insured, 24/7 Available).

Purpose: The hero is the first thing a visitor sees. A real machine photo immediately communicates "heavy equipment contractor" — the gradient communicates nothing. Trust badges are CONV-02 and CONV-03 requirements.
Output: Hero.tsx with photo background, dark overlay, and three pill trust badges.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-dark-theme-homepage/04-RESEARCH.md
@abc-roofing/src/components/Hero.tsx
@.planning/phases/04-dark-theme-homepage/04-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace gradient background with photo + overlay in Hero.tsx</name>
  <files>abc-roofing/src/components/Hero.tsx</files>
  <action>
Add `import Image from "next/image";` at the top of the file (after the existing `import Link` and `import { useState }` lines).

Replace the entire background section of the Hero section element. Currently:
```tsx
<section className="relative min-h-[100svh] flex items-center overflow-hidden bg-[#0f1f2d]">
  {/* Optimized gradient background - reduced blur for mobile performance */}
  <div className="absolute inset-0">
    <div className="absolute inset-0 bg-gradient-to-br from-[#0f1f2d] via-[#2d4f64] to-[#0f1f2d]" />
    <div className="absolute top-0 right-0 w-[400px] md:w-[800px] h-[400px] md:h-[800px] bg-[#F5A623]/15 md:bg-[#F5A623]/20 rounded-full blur-[80px] md:blur-[150px] -translate-y-1/2 translate-x-1/2" />
  </div>

  {/* Grid pattern overlay */}
  <div className="absolute inset-0 bg-[linear-gradient(rgba(255,255,255,0.02)_1px,transparent_1px),linear-gradient(90deg,rgba(255,255,255,0.02)_1px,transparent_1px)] bg-[size:40px_40px] md:bg-[size:60px_60px]" />
```

Replace with:
```tsx
<section className="relative min-h-[100svh] flex items-center overflow-hidden bg-[#0f1f2d]">
  {/* Machine photo background */}
  <Image
    src="/images/hero-excavator.jpg"
    alt=""
    fill
    priority
    className="object-cover object-center"
    sizes="100vw"
  />
  {/* Dark overlay for text legibility */}
  <div className="absolute inset-0 bg-[#0f1f2d]/70" />
  {/* Subtle gradient for bottom legibility */}
  <div className="absolute inset-0 bg-gradient-to-t from-[#0f1f2d]/40 to-transparent" />
  {/* Grid pattern overlay */}
  <div className="absolute inset-0 bg-[linear-gradient(rgba(255,255,255,0.02)_1px,transparent_1px),linear-gradient(90deg,rgba(255,255,255,0.02)_1px,transparent_1px)] bg-[size:40px_40px] md:bg-[size:60px_60px]" />
```

The `bg-[#0f1f2d]` on the section itself acts as a fallback color while the photo loads — keep it.

Do NOT change any other part of the Hero: the container div, the left content column, the h1, the CTA buttons, the desktop form, the mobile expandable form, the stats bar, or any form logic. Preserve the formStatus state machine and handleSubmit verbatim.
  </action>
  <verify>
`cd abc-roofing && npm run build` passes. Grep Hero.tsx for `fill` — must return the Image component fill prop. Grep Hero.tsx for `priority` — must return the Image component priority prop. Grep Hero.tsx for `bg-gradient-to-br` — must return 0 results (the gradient is gone).
  </verify>
  <done>Hero.tsx imports Image from next/image. The photo layer uses fill + priority + sizes="100vw". The gradient background divs are removed. Dark overlay div is present. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade trust indicators to pill badges (CONV-02, CONV-03)</name>
  <files>abc-roofing/src/components/Hero.tsx</files>
  <action>
Replace the existing trust indicators section (the `flex flex-wrap items-center justify-center lg:justify-start gap-4 md:gap-6 text-white/60` div with three plain text items) with upgraded pill badges.

The current trust indicators section (around line 66-85 in the original file) shows three plain flex items with check icons: "WCB Alberta Compliant", "Alberta One-Call Certified", "Free Site Estimates".

Replace the entire trust indicators `&lt;div&gt;` with this new version:
```tsx
{/* Trust badge pills - CONV-02, CONV-03 */}
<div className="flex flex-wrap items-center justify-center lg:justify-start gap-3">
  <div className="flex items-center gap-2 bg-white/10 border border-white/10 px-3 py-2 rounded-lg">
    <svg className="w-4 h-4 text-[#F5A623] flex-shrink-0" fill="currentColor" viewBox="0 0 20 20">
      <path fillRule="evenodd" d="M2.166 4.999A11.954 11.954 0 0010 1.944 11.954 11.954 0 0017.834 5c.11.65.166 1.32.166 2.001 0 5.225-3.34 9.67-8 11.317C5.34 16.67 2 12.225 2 7c0-.682.057-1.35.166-2.001zm11.541 3.708a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clipRule="evenodd" />
    </svg>
    <span className="text-xs font-semibold text-white/90 whitespace-nowrap">Licensed &amp; Insured</span>
  </div>
  <div className="flex items-center gap-2 bg-white/10 border border-white/10 px-3 py-2 rounded-lg">
    <svg className="w-4 h-4 text-[#F5A623] flex-shrink-0" fill="currentColor" viewBox="0 0 20 20">
      <path fillRule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clipRule="evenodd" />
    </svg>
    <span className="text-xs font-semibold text-white/90 whitespace-nowrap">WCB Alberta</span>
  </div>
  <div className="flex items-center gap-2 bg-white/10 border border-white/10 px-3 py-2 rounded-lg">
    <svg className="w-4 h-4 text-[#F5A623] flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z" />
    </svg>
    <span className="text-xs font-semibold text-white/90 whitespace-nowrap">24/7 Available</span>
  </div>
</div>
```

These three badges satisfy CONV-02 (Licensed & Insured, WCB Alberta visible on homepage) and CONV-03 (24/7 Available prominent).
  </action>
  <verify>
`cd abc-roofing && npm run build` passes. Grep Hero.tsx for "Licensed" — must return the badge text. Grep Hero.tsx for "WCB Alberta" — must return the badge text. Grep Hero.tsx for "24/7 Available" — must return the badge text.
  </verify>
  <done>Three pill badge trust indicators replace the plain text trust bar. Build passes. All three badge texts present in Hero.tsx.</done>
</task>

</tasks>

<verification>
1. `cd abc-roofing && npm run build` passes with no errors
2. Hero.tsx contains `import Image from "next/image"`
3. Hero.tsx contains `fill` and `priority` on the Image component
4. Hero.tsx contains `src="/images/hero-excavator.jpg"`
5. Hero.tsx contains the three trust badge divs (Licensed & Insured, WCB Alberta, 24/7 Available)
6. Hero.tsx does NOT contain `bg-gradient-to-br` (the gradient background is gone)
</verification>

<success_criteria>
- Hero photo background: next/image fill + priority + sizes="100vw" at /images/hero-excavator.jpg
- Dark overlay: bg-[#0f1f2d]/70 absolute inset-0
- Three trust pill badges visible: Licensed & Insured, WCB Alberta, 24/7 Available
- Form logic (handleSubmit, formStatus, loading/success states) completely unchanged
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/04-dark-theme-homepage/04-03-SUMMARY.md` using the summary template.
</output>
