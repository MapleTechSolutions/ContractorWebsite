---
phase: 04-dark-theme-homepage
plan: "07"
type: execute
wave: 5
depends_on: ["04-03", "04-04", "04-05", "04-06"]
files_modified:
  - abc-roofing/src/app/page.tsx
  - abc-roofing/src/components/MobileStickyCTA.tsx
autonomous: false

must_haves:
  truths:
    - "The homepage renders sections in order: Hero, ServicePillars, WhyBigCountry, Testimonials, CTABanner"
    - "AboutPreview is not rendered on the homepage"
    - "The mobile sticky CTA bar has a dark background and white 'Call Now' button text"
    - "The complete site loads without errors on npm run build"
    - "All five homepage sections are visible in the browser at the correct viewport"
  artifacts:
    - path: "abc-roofing/src/app/page.tsx"
      provides: "Homepage with new component order"
      contains: "ServicePillars"
      exports: ["default function Home"]
    - path: "abc-roofing/src/components/MobileStickyCTA.tsx"
      provides: "Dark mobile sticky CTA bar"
      contains: "bg-[#0f1f2d]"
      exports: ["default function MobileStickyCTA"]
  key_links:
    - from: "abc-roofing/src/app/page.tsx"
      to: "abc-roofing/src/components/ServicePillars.tsx"
      via: "import and JSX render"
      pattern: "import ServicePillars"
    - from: "abc-roofing/src/app/page.tsx"
      to: "abc-roofing/src/components/CTABanner.tsx"
      via: "import and JSX render"
      pattern: "import CTABanner"
    - from: "abc-roofing/src/components/MobileStickyCTA.tsx"
      to: "body background context"
      via: "bg-[#0f1f2d] matching body bg"
      pattern: "bg-\\[#0f1f2d\\]"
---

<objective>
Wire the new homepage components into page.tsx (replacing the old component set) and flip MobileStickyCTA.tsx to dark colors. This is the final wiring step that makes all Phase 4 work visible as a cohesive dark homepage.

Purpose: All new components exist but aren't rendered yet. page.tsx still imports and renders the old light-theme components. MobileStickyCTA still has a white background that breaks the dark theme on mobile. This plan connects everything.
Output: page.tsx with new 5-component homepage; MobileStickyCTA.tsx with dark styling.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-dark-theme-homepage/04-RESEARCH.md
@abc-roofing/src/app/page.tsx
@abc-roofing/src/components/MobileStickyCTA.tsx
@.planning/phases/04-dark-theme-homepage/04-03-SUMMARY.md
@.planning/phases/04-dark-theme-homepage/04-04-SUMMARY.md
@.planning/phases/04-dark-theme-homepage/04-05-SUMMARY.md
@.planning/phases/04-dark-theme-homepage/04-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite page.tsx with new 5-component homepage section order</name>
  <files>abc-roofing/src/app/page.tsx</files>
  <action>
Replace the entire contents of abc-roofing/src/app/page.tsx with:

```tsx
import Hero from "@/components/Hero";
import ServicePillars from "@/components/ServicePillars";
import WhyBigCountry from "@/components/WhyBigCountry";
import Testimonials from "@/components/Testimonials";
import CTABanner from "@/components/CTABanner";

export default function Home() {
  return (
    <>
      <Hero />
      <ServicePillars />
      <WhyBigCountry />
      <Testimonials />
      <CTABanner />
    </>
  );
}
```

This:
- Removes imports for ServicesPreview, AboutPreview, WhyChooseUs, TestimonialsPreview, CTASection from the homepage (they still exist in src/components/ for other pages)
- Adds the new components in the correct spec order
- Renders 5 sections as required (HOME-01 through HOME-03, HOME-06, HOME-07)

Do not modify ServicesPreview.tsx, AboutPreview.tsx, WhyChooseUs.tsx, TestimonialsPreview.tsx, or CTASection.tsx.
  </action>
  <verify>
`cd abc-roofing && npm run build` passes. Grep page.tsx for "ServicePillars" — must be present. Grep page.tsx for "AboutPreview" — must return 0 results. Grep page.tsx for "ServicesPreview" — must return 0 results.
  </verify>
  <done>page.tsx imports and renders exactly: Hero, ServicePillars, WhyBigCountry, Testimonials, CTABanner. No old components referenced. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Flip MobileStickyCTA.tsx to dark colors</name>
  <files>abc-roofing/src/components/MobileStickyCTA.tsx</files>
  <action>
Make targeted color changes to MobileStickyCTA.tsx. Keep all logic unchanged: isVisible/isAtBottom state, scroll useEffect, footer detection, `if (!isVisible || isAtBottom) return null` guard.

**Outer div (currently line ~31, `bg-white border-t border-gray-200 shadow-[0_-4px_20px_rgba(0,0,0,0.1)]`):**
```
// FROM:
className="fixed bottom-0 left-0 right-0 z-40 md:hidden bg-white border-t border-gray-200 shadow-[0_-4px_20px_rgba(0,0,0,0.1)] safe-area-bottom"

// TO:
className="fixed bottom-0 left-0 right-0 z-40 md:hidden bg-[#0f1f2d] border-t border-white/10 shadow-[0_-4px_20px_rgba(0,0,0,0.4)] safe-area-bottom"
```

**Call Now button (the phone link, currently `text-[#0f1f2d] bg-gray-100 active:bg-gray-200`):**
```
// FROM:
className="flex-1 flex items-center justify-center gap-2 py-3.5 rounded-xl font-bold text-[#0f1f2d] bg-gray-100 active:bg-gray-200 transition-colors touch-manipulation"

// TO:
className="flex-1 flex items-center justify-center gap-2 py-3.5 rounded-xl font-bold text-white bg-white/10 active:bg-white/20 transition-colors touch-manipulation"
```

**Get a Quote link button** — already correctly styled (`bg-[#F5A623] text-[#0f1f2d]`). Do not change it.

The SVG phone icon inside the Call Now button inherits color from its parent. No fill/stroke color change needed there.

Also update globals.css `.sticky-cta-mobile` class — it was updated in Plan 01 already. This component uses inline classNames directly, not the utility class, so this is purely a component change.
  </action>
  <verify>
`cd abc-roofing && npm run build` passes. Grep MobileStickyCTA.tsx for `bg-white` (standalone) — must return 0 results. Grep for `bg-\[#0f1f2d\]` — must be present on the outer div. Grep for `bg-gray-100` — must return 0 results.
  </verify>
  <done>MobileStickyCTA outer wrapper is bg-[#0f1f2d] with border-white/10. Call Now button is white text on bg-white/10. Get a Quote button unchanged (amber). Build passes.</done>
</task>

</tasks>

<verification>
Full Phase 4 build verification:
1. `cd abc-roofing && npm run build` — must pass with zero errors
2. `grep "bg-white[^/]" abc-roofing/src/components/MobileStickyCTA.tsx` → 0 results
3. `grep "ServicesPreview\|AboutPreview\|WhyChooseUs\|TestimonialsPreview\|CTASection" abc-roofing/src/app/page.tsx` → 0 results
4. `grep "ServicePillars\|WhyBigCountry\|CTABanner" abc-roofing/src/app/page.tsx` → 3 results
</verification>

<success_criteria>
- page.tsx renders exactly: Hero, ServicePillars, WhyBigCountry, Testimonials, CTABanner — nothing else
- MobileStickyCTA: dark outer wrapper (bg-[#0f1f2d]), white Call Now button text
- npm run build passes with zero errors
- Old light components (ServicesPreview, AboutPreview, etc.) still exist in src/components/ but are not imported in page.tsx
</success_criteria>

<output>
After completion, create `.planning/phases/04-dark-theme-homepage/04-07-SUMMARY.md` using the summary template.

Then, run the dev server and conduct a human verification checkpoint:
</output>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Full Phase 4 dark theme homepage:
    - globals.css: dark body background
    - layout.tsx: dark colorScheme
    - Header.tsx: always-dark with dark mobile menu overlay
    - public/images/: hero-excavator.jpg, excavation-pillar.jpg, snow-pillar.jpg
    - Hero.tsx: real machine photo background with trust badge pills
    - ServicePillars.tsx: two photo pillars (Excavation, Snow)
    - WhyBigCountry.tsx: 4 numbered trust reasons
    - Testimonials.tsx: dark-themed slider
    - CTABanner.tsx: dark CTA section
    - MobileStickyCTA.tsx: dark sticky bar
    - page.tsx: wired to new components
  </what-built>
  <how-to-verify>
    1. Run the dev server: cd abc-roofing && npm run dev
    2. Open http://localhost:3000 in a browser

    Desktop checks (1280px+):
    - [ ] Page background is dark — no white or light-grey areas visible as the primary surface
    - [ ] Hero shows a machine photo (not a blue gradient) — dark overlay makes headline readable
    - [ ] Header is dark — not transparent-to-white on scroll
    - [ ] Trust badges (Licensed & Insured, WCB Alberta, 24/7 Available) visible in hero
    - [ ] Service Pillars section shows two photo cards side-by-side
    - [ ] Why Big Country section shows 4 numbered cards with amber numbers (01-04) in 4 columns
    - [ ] Testimonials section is dark-background, not white
    - [ ] CTA Banner section is dark with bold headline
    - [ ] Footer is dark (unchanged — should already be correct)
    - [ ] Scroll down — header stays dark throughout

    Mobile checks (resize to 390px or use DevTools):
    - [ ] All sections stack correctly (single column)
    - [ ] Hero CTA buttons are full-width
    - [ ] Scroll down — sticky CTA bar appears at bottom with dark background
    - [ ] Tap the hamburger — mobile menu opens as dark overlay with white nav links
    - [ ] Why Big Country shows 2-column grid on mobile (not 4-column)

    Photo assignment check:
    - The correct photo should be in the hero (ideally the Kobelco excavator shot)
    - If photos are misassigned (e.g., snow photo in hero slot), note it — this can be corrected by swapping filenames in public/images/

    Form check:
    - [ ] Desktop: Fill out the hero form and submit — see loading spinner then success screen
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 4, or describe any visual issues to fix before marking complete.</resume-signal>
</task>
