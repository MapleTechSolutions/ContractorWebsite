---
phase: 04-dark-theme-homepage
plan: "05"
type: execute
wave: 4
depends_on: ["04-01"]
files_modified:
  - abc-roofing/src/components/WhyBigCountry.tsx
  - abc-roofing/src/components/CTABanner.tsx
autonomous: true

must_haves:
  truths:
    - "A 'Why Big Country' section shows exactly 4 numbered reasons (01–04) with large prominent amber numbers, white titles, and muted white descriptions"
    - "The 4 reasons are: 01 24/7 Availability, 02 Licensed & Insured, 03 Commercial-Grade Equipment, 04 Alberta-Based"
    - "The section has a 2-column grid on mobile (grid-cols-2) and 4-column on desktop (lg:grid-cols-4)"
    - "A CTA banner section shows a large dark section with bold headline, amber 'Get a Quote' button, and white 'Call' button"
    - "Both are server components — no 'use client' directive"
  artifacts:
    - path: "abc-roofing/src/components/WhyBigCountry.tsx"
      provides: "4-reason numbered trust section with accent numbers"
      contains: "01"
      exports: ["default function WhyBigCountry"]
    - path: "abc-roofing/src/components/CTABanner.tsx"
      provides: "Final CTA banner before footer"
      contains: "Commercial Contract"
      exports: ["default function CTABanner"]
  key_links:
    - from: "abc-roofing/src/components/WhyBigCountry.tsx"
      to: "VSRL-04"
      via: "accent-colored prominent numbers text-[#F5A623]"
      pattern: "text-\\[#F5A623\\].*01"
    - from: "abc-roofing/src/components/CTABanner.tsx"
      to: "CONV-01"
      via: "tel: link"
      pattern: "tel:\\+15875551234"
---

<objective>
Create WhyBigCountry.tsx (HOME-03) and CTABanner.tsx (HOME-07) as new server components. WhyBigCountry replaces WhyChooseUs on the homepage with exactly 4 trust reasons and prominent amber numbers. CTABanner replaces CTASection on the homepage as a standalone component.

Purpose: The spec requires exactly 4 reasons (not 6), prominent foreground accent numbers (not the faded /10 opacity watermark in the current WhyChooseUs), and the CTA must be its own component separate from WhyBigCountry. Creating new components avoids breaking WhyChooseUs and CTASection which may be used on other pages.
Output: WhyBigCountry.tsx and CTABanner.tsx as server components.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-dark-theme-homepage/04-RESEARCH.md
@abc-roofing/src/components/WhyChooseUs.tsx
@abc-roofing/src/components/CTASection.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create WhyBigCountry.tsx with 4 prominent numbered reasons</name>
  <files>abc-roofing/src/components/WhyBigCountry.tsx</files>
  <action>
Create `abc-roofing/src/components/WhyBigCountry.tsx` as a new server component (no `"use client"`).

No imports needed beyond what's standard (this is a pure display component with no links needed beyond what's in the section itself).

The 4 reasons data (inline, no external file):
```
{ number: "01", title: "24/7 Availability", description: "Storm hits at 3 AM? We're dispatched. Commercial snow contracts include around-the-clock storm response — because downtime costs your business money." }
{ number: "02", title: "Licensed & Insured", description: "Fully licensed contractor, WCB Alberta registered, and carrying commercial liability insurance. Your site liability is covered from day one." }
{ number: "03", title: "Commercial-Grade Equipment", description: "Our fleet handles any scale — from single-lot clearing to multi-site contracts. Kobelco excavators, Deere track loaders, and more on the job." }
{ number: "04", title: "Alberta-Based", description: "We operate in Big Country, Alberta. We know the land, the regulations, and the Alberta One-Call requirements. No out-of-province crews." }
```

Full component:
```tsx
export default function WhyBigCountry() {
  const reasons = [
    {
      number: "01",
      title: "24/7 Availability",
      description: "Storm hits at 3 AM? We're dispatched. Commercial snow contracts include around-the-clock storm response — because downtime costs your business money.",
    },
    {
      number: "02",
      title: "Licensed & Insured",
      description: "Fully licensed contractor, WCB Alberta registered, and carrying commercial liability insurance. Your site liability is covered from day one.",
    },
    {
      number: "03",
      title: "Commercial-Grade Equipment",
      description: "Our fleet handles any scale — from single-lot clearing to multi-site contracts. Kobelco excavators, Deere track loaders, and more on the job.",
    },
    {
      number: "04",
      title: "Alberta-Based",
      description: "We operate in Big Country, Alberta. We know the land, the regulations, and Alberta One-Call requirements. No out-of-province crews.",
    },
  ];

  return (
    <section id="why-us" className="section-padding bg-[#0f1f2d] relative overflow-hidden">
      {/* Subtle amber glow */}
      <div className="absolute top-0 right-0 w-[300px] md:w-[500px] h-[300px] md:h-[500px] bg-[#F5A623]/8 rounded-full blur-[80px] md:blur-[120px]" />

      <div className="container-custom relative z-10">
        {/* Section header */}
        <div className="text-center mb-10 md:mb-14">
          <div className="inline-flex items-center gap-2 bg-white/10 text-[#F5A623] px-4 md:px-5 py-2 rounded-full text-sm font-semibold mb-4 md:mb-6">
            Why Big Country
          </div>
          <h2 className="text-2xl sm:text-3xl md:text-4xl lg:text-5xl font-black text-white tracking-tight mb-4">
            The Big Country
            <span className="text-[#F5A623]"> Difference</span>
          </h2>
          <p className="text-base md:text-lg text-white/60 max-w-2xl mx-auto">
            Licensed, insured, and WCB compliant — the credentials and heavy equipment your commercial site requires.
          </p>
        </div>

        {/* 4-reason grid */}
        <div className="grid grid-cols-2 lg:grid-cols-4 gap-4 md:gap-6">
          {reasons.map((reason) => (
            <div
              key={reason.number}
              className="p-6 md:p-8 bg-white/5 border border-white/10 rounded-xl"
            >
              <div className="text-4xl md:text-5xl font-black text-[#F5A623] mb-3">
                {reason.number}
              </div>
              <h3 className="text-base md:text-lg font-black text-white mb-2 leading-tight">
                {reason.title}
              </h3>
              <p className="text-white/60 leading-relaxed text-sm">
                {reason.description}
              </p>
            </div>
          ))}
        </div>
      </div>
    </section>
  );
}
```
  </action>
  <verify>
`cd abc-roofing && npm run build` passes. Grep WhyBigCountry.tsx for `"01"` and `"04"` — both must be present. Grep for `text-[#F5A623]` on the number div — must be present. Grep for `grid-cols-2 lg:grid-cols-4` — must be present. Grep for `"use client"` — must return 0 results.
  </verify>
  <done>WhyBigCountry.tsx is a server component with 4 reasons, prominent amber numbers, 2-col mobile / 4-col desktop grid. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Create CTABanner.tsx as standalone dark CTA section</name>
  <files>abc-roofing/src/components/CTABanner.tsx</files>
  <action>
Create `abc-roofing/src/components/CTABanner.tsx` as a new server component (no `"use client"`).

Imports needed:
```tsx
import Link from "next/link";
```

Model after the existing CTASection.tsx but with these differences:
- Headline at `text-4xl md:text-5xl font-black` (heavier than CTASection's text-3xl)
- Trust badges row shows: "Licensed & Insured", "WCB Alberta", "24/7 Available" (3 badges, matches the Phase 4 spec)
- Keep the grid pattern overlay from CTASection
- Keep the amber glow decorative div from CTASection
- Structure is identical to CTASection — this is essentially CTASection with a weight bump on the headline

Full component:
```tsx
import Link from "next/link";

export default function CTABanner() {
  return (
    <section className="py-20 bg-[#0f1f2d] relative overflow-hidden">
      {/* Background elements */}
      <div className="absolute top-0 right-0 w-[600px] h-[600px] bg-[#F5A623]/20 rounded-full blur-[150px]" />
      <div className="absolute bottom-0 left-0 w-[400px] h-[400px] bg-[#0f1f2d]/30 rounded-full blur-[100px]" />
      {/* Grid pattern */}
      <div className="absolute inset-0 bg-[linear-gradient(rgba(255,255,255,0.02)_1px,transparent_1px),linear-gradient(90deg,rgba(255,255,255,0.02)_1px,transparent_1px)] bg-[size:60px_60px]" />

      <div className="container-custom relative z-10">
        <div className="max-w-4xl mx-auto text-center">
          <h2 className="text-4xl md:text-5xl font-black text-white mb-6 tracking-tight">
            Ready to Book Your
            <span className="text-[#F5A623]"> Commercial Contract?</span>
          </h2>
          <p className="text-xl text-white/70 mb-10 max-w-2xl mx-auto">
            Get a free site estimate from our team. We serve commercial and industrial clients across Big Country, Alberta. Response within one business day.
          </p>

          <div className="flex flex-col sm:flex-row gap-4 justify-center mb-12">
            <Link href="/contact" className="btn-primary text-base md:text-lg">
              Get a Quote
              <svg className="w-5 h-5 ml-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M17 8l4 4m0 0l-4 4m4-4H3" />
              </svg>
            </Link>
            <a
              href="tel:+15875551234"
              aria-label="Call (587) 555-1234"
              className="btn-outline-white text-base md:text-lg"
            >
              <svg className="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z" />
              </svg>
              (587) 555-1234
            </a>
          </div>

          {/* Trust badges */}
          <div className="flex flex-wrap items-center justify-center gap-6 md:gap-8">
            <div className="flex items-center gap-2 text-white/70">
              <svg className="w-5 h-5 text-[#F5A623]" fill="currentColor" viewBox="0 0 20 20">
                <path fillRule="evenodd" d="M2.166 4.999A11.954 11.954 0 0010 1.944 11.954 11.954 0 0017.834 5c.11.65.166 1.32.166 2.001 0 5.225-3.34 9.67-8 11.317C5.34 16.67 2 12.225 2 7c0-.682.057-1.35.166-2.001zm11.541 3.708a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clipRule="evenodd" />
              </svg>
              <span className="font-semibold text-sm md:text-base">Licensed &amp; Insured</span>
            </div>
            <div className="flex items-center gap-2 text-white/70">
              <svg className="w-5 h-5 text-[#F5A623]" fill="currentColor" viewBox="0 0 20 20">
                <path fillRule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clipRule="evenodd" />
              </svg>
              <span className="font-semibold text-sm md:text-base">WCB Alberta Compliant</span>
            </div>
            <div className="flex items-center gap-2 text-white/70">
              <svg className="w-5 h-5 text-[#F5A623]" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z" />
              </svg>
              <span className="font-semibold text-sm md:text-base">24/7 Available</span>
            </div>
          </div>
        </div>
      </div>
    </section>
  );
}
```
  </action>
  <verify>
`cd abc-roofing && npm run build` passes. Grep CTABanner.tsx for `tel:+15875551234` — must be present (CONV-01). Grep for `text-4xl md:text-5xl font-black` — must be present. Grep for `"use client"` — must return 0 results.
  </verify>
  <done>CTABanner.tsx is a server component with font-black headline, tel: link, and three trust badges. Build passes.</done>
</task>

</tasks>

<verification>
1. `cd abc-roofing && npm run build` passes
2. WhyBigCountry.tsx: 4 reasons, amber numbers at `text-[#F5A623]`, no "use client"
3. CTABanner.tsx: `text-4xl md:text-5xl font-black`, `tel:+15875551234`, no "use client"
4. Both files exist in abc-roofing/src/components/
5. WhyChooseUs.tsx and CTASection.tsx remain unchanged
</verification>

<success_criteria>
- WhyBigCountry.tsx: 4 reasons (01-04), prominent amber numbers (not opacity /10 watermark), grid-cols-2 lg:grid-cols-4, server component
- CTABanner.tsx: font-black headline, get-a-quote + tel: buttons, 3 trust badges, server component
- Build passes
- Existing WhyChooseUs.tsx and CTASection.tsx unmodified
</success_criteria>

<output>
After completion, create `.planning/phases/04-dark-theme-homepage/04-05-SUMMARY.md` using the summary template.
</output>
